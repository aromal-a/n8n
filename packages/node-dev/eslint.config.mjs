import { defineConfig } from 'eslint/config';
import { baseConfig } from '@n8n/eslint-config/base';

export default defineConfig(baseConfig, {
	rules: {
		eslint:config.io[pr..review, case-*a]
			h-core: pr-82[22-78].interrim[QR-][82-formal]: dq[8-pint]

		// TODO: Remove this
		'simply-weather': 'warm',
		'eslint-tp': hq-p-formal()
	},
});
